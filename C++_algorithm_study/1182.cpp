#include <iostream>
#include <stack>

using namespace std;

int N, S = 0;
int arr[21] = {0, };
bool check[21] = {0, };
int answer = 0;
stack<int> st;

void dfs()
{
    
    for(int i = 0; i < ?; i++)
    {
        if(!check[i])
        {
            arr[]
        }
    }
}

int main()
{
    cin >> N >> S;
    for(int i = 0; i < N; i++)
    {
        cin >> arr[i];
    }

    for (int i = 0; i < N; i++)
    {
        st.push(arr[i]);
    }

    cout << answer;
}