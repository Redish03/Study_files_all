#include <iostream>

using namespace std;

int n, m;
int combin;

int dp(int x, int y)
{
    for(int i = 0; i < y; i++)
    {
        combin *= x
        x--;
    }
    
    return 0;
}

int main()
{
    cin >> n >> m;

    cin << dp(n, m);
}