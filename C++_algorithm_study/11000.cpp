#include <iostream>
#include <queue>

using namespace std;

int main()
{
    int N, answer = 0;
    priority_queue<pair<int, int>> time;
    cin >> N;

    for (int i = 0; i < N; i++)
    {
        int S, T;

        time.push(make_pair(S, T));

    }
}