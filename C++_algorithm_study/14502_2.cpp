#include <iostream>
#include <queue>
#include <vector>

using namespace std;

int N, M;
queue<pair<int, int>> q;
int arr[8][8];
int dx[] = {0, 0, -1, 1};
int dy[] = {1, -1, 0, 0};
int check[8][8] = {0, };


int main()
{
    cin >> N >> M;
    for (int i = 0; i < N; i++)
    {
        for (int j = 0; j < M; j++)
        {
            cin >> arr[i][j];  
            if()  
        }
    }

}