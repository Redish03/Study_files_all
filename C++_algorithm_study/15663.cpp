#include <iostream>
#include <algorithm>

using namespace std;

int arr[10];
int N, M;
int remember[100][100] = {
    0,
};

bool check_duplicate()
{
    for (int i = 0; i < M; i++) {
        for()
    }
}

void find_arr()
{
}

int main()
{
    cin >> N >> M;
    for (int i = 0; i < N; i++)
    {
        cin >> arr[i];
    }
    sort(arr, arr + N);
}