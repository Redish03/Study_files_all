#include <iostream>
#include <vector>

using namespace std;

int N, M;
vector<int> v;

int main()
{
    cin >> N >> M;

    int people_cnt;
    cin >> people_cnt;
    for (int i = 0; i < people_cnt; i++)
    {
        int a;
        cin >> a;
        v.push_back(a);
    }

    for (int i = 0; i < M; i++)
    {
        int num;
        cin >> num;
        for (int j = 0; j < num; j++)
        {
            
        }
    }
}