#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int arr[1001];
int Memory[1001];
int N;
int count = 0;
vector<int> answer;
int Max;

int dp(int x, int lastnum)
{
    for (int i = x; i < ;)
}

int main()
{
    cin >> N;
    for (int i = 0; i < N; i++)
    {
        cin >> arr[i];
    }

    dp(N - 1, arr[N - 1]);

    cout << 0;
}
